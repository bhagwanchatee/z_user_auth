# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "4.0"
metadata:
  name: zpurreq3
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          destination: 0M6F5TC_SAML_ASSERTION_DEV080
          url: https://my420245.s4hana.cloud.sap
          authenticationType: reentranceTicket # SAML support for vscode
        app:
          name: ZPURREG3
          description: Purchase Register Report – MM  New
          package: zmm_purchase_register
          transport: MRFK902178
        exclude:
          - /test/
